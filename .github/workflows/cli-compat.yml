name: Agent CLI compatibility

on:
  push:
    branches: [main]
    paths:
      - '.agents/**'
      - '.github/workflows/cli-compat.yml'
      - 'scripts/**'
      - 'skills/**'
  pull_request:
    paths:
      - '.agents/**'
      - '.github/workflows/cli-compat.yml'
      - 'scripts/**'
      - 'skills/**'
  schedule:
    - cron: '23 9 * * 1'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cli-compat-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  codex:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Install current Codex CLI
        run: npm install --global @openai/codex@latest
      - name: Run real Codex CLI discovery test
        run: bash scripts/test-codex-cli-e2e.sh
